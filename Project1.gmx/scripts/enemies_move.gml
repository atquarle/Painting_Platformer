for (i = 0; i < global.enemies_length; i+= 1) {
    with (instance_find(obj_enemy, global.enemies[i])) {
        /*dir = direction;
        path=path_add();
        //mp_grid_add_instances(grid,obj_wall,1); //Collision
        mp_grid_path(grid,path,x,y,obj_player.x,obj_player.y,1);
        path_start(path,5,"",1);*/
        
    }
}
